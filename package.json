{
  "name": "chaos-score-simulator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && npx serve dist -l 3000",
    "start": "npm run build && npx serve dist -l 3000",
    "build": "node build.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.20.2",
    "fs-extra": "^11.2.0",
    "typescript": "^5.3.3"
  }
}